# Encrypted Report Viewer

This repository hosts an encrypted single-page report viewer. The report data is embedded as an AES-GCM encrypted payload and requires the shared password to open in the browser.
